/**
 * Window event dispatched by UI (the HUD button) to toggle the terminal.
 * Keeps the launcher decoupled from TerminalOverlay's open state.
 */
export const TERMINAL_TOGGLE_EVENT = "terminal:toggle";
